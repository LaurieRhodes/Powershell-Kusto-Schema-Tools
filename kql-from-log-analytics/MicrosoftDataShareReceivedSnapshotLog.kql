// ============================================================================
// Azure Data Explorer KQL Script for MicrosoftDataShareReceivedSnapshotLog - UPDATED VERSION
// ============================================================================
// Generated: 2025-09-17 07:26:49
// Table type: Microsoft
// Schema discovered using hybrid approach (Management API + getschema)
// Data type corrections applied: TenantId->guid, Double->real (empirical fixes)
// All columns including underscore columns included
// Original columns: 20, Final columns: 20
// ============================================================================

.create-merge table MicrosoftDataShareReceivedSnapshotLogRaw (records:dynamic)

.alter-merge table MicrosoftDataShareReceivedSnapshotLogRaw policy retention softdelete = 1d

.alter table MicrosoftDataShareReceivedSnapshotLogRaw policy caching hot = 1h

// JSON mapping - choose appropriate option based on data structure
.create-or-alter table MicrosoftDataShareReceivedSnapshotLogRaw ingestion json mapping 'MicrosoftDataShareReceivedSnapshotLogRawMapping' '[{"column":"records","Properties":{"path":"$.records"}}]'
// Alternative for direct events: '[{"column":"records","Properties":{"path":"$"}}]'

.create-merge table MicrosoftDataShareReceivedSnapshotLog(
TimeGenerated:datetime,
TenantId:guid,
SizeWritten:string,
FilesRead:string,
FilesWritten:string,
DataSetName:string,
DataSetType:string,
DataSetMappingType:string,
SizeRead:string,
TriggerType:string,
EndTime:string,
StartTime:string,
DetailMessage:string,
CorrelationId:string,
Category:string,
OperationName:string,
Status:string,
SourceSystem:string,
Type:string,
_ResourceId:string,
_TimeReceived:datetime)

.alter table MicrosoftDataShareReceivedSnapshotLog policy caching hot = 1d

.create-or-alter function MicrosoftDataShareReceivedSnapshotLogExpand() {
MicrosoftDataShareReceivedSnapshotLogRaw
| mv-expand events = records
// Alternative for non-nested: | extend events = records
| project
TimeGenerated=todatetime(events.TimeGenerated),
TenantId=toguid(events.TenantId),
SizeWritten=tostring(events.SizeWritten),
FilesRead=tostring(events.FilesRead),
FilesWritten=tostring(events.FilesWritten),
DataSetName=tostring(events.DataSetName),
DataSetType=tostring(events.DataSetType),
DataSetMappingType=tostring(events.DataSetMappingType),
SizeRead=tostring(events.SizeRead),
TriggerType=tostring(events.TriggerType),
EndTime=tostring(events.EndTime),
StartTime=tostring(events.StartTime),
DetailMessage=tostring(events.DetailMessage),
CorrelationId=tostring(events.CorrelationId),
Category=tostring(events.Category),
OperationName=tostring(events.OperationName),
Status=tostring(events.Status),
SourceSystem=tostring(events.SourceSystem),
Type=tostring(events.Type),
_ResourceId=tostring(events._ResourceId),
_TimeReceived=todatetime(now())
}

.alter table MicrosoftDataShareReceivedSnapshotLog policy update @'[{"Source": "MicrosoftDataShareReceivedSnapshotLogRaw", "Query": "MicrosoftDataShareReceivedSnapshotLogExpand()", "IsEnabled": "True", "IsTransactional": true}]'
