// ============================================================================
// Azure Data Explorer KQL Script for HDInsightHiveAndLLAPLogs - UPDATED VERSION
// ============================================================================
// Generated: 2025-09-14 10:15:11
// Table type: Microsoft
// Schema discovered using hybrid approach (Management API + getschema)
// Data type corrections applied: TenantId->guid, Double->real (empirical fixes)
// All columns including underscore columns included
// Original columns: 13, Final columns: 13
// ============================================================================

.create-merge table HDInsightHiveAndLLAPLogsRaw (records:dynamic)

.alter-merge table HDInsightHiveAndLLAPLogsRaw policy retention softdelete = 1d

.alter table HDInsightHiveAndLLAPLogsRaw policy caching hot = 1h

// JSON mapping - choose appropriate option based on data structure
.create-or-alter table HDInsightHiveAndLLAPLogsRaw ingestion json mapping 'HDInsightHiveAndLLAPLogsRawMapping' '[{"column":"records","Properties":{"path":"$.records"}}]'
// Alternative for direct events: '[{"column":"records","Properties":{"path":"$"}}]'

.create-merge table HDInsightHiveAndLLAPLogs(
TimeGenerated:datetime,
TenantId:guid,
CorrelationId:string,
LogLevel:string,
Message:string,
ClusterName:string,
ClusterType:string,
HostName:string,
LogType:string,
OperationName:string,
SourceSystem:string,
Type:string,
_ResourceId:string,
_TimeReceived:datetime)

.alter table HDInsightHiveAndLLAPLogs policy caching hot = 1d

.create-or-alter function HDInsightHiveAndLLAPLogsExpand() {
HDInsightHiveAndLLAPLogsRaw
| mv-expand events = records
// Alternative for non-nested: | extend events = records
| project
TimeGenerated=todatetime(events.TimeGenerated),
TenantId=toguid(events.TenantId),
CorrelationId=tostring(events.CorrelationId),
LogLevel=tostring(events.LogLevel),
Message=tostring(events.Message),
ClusterName=tostring(events.ClusterName),
ClusterType=tostring(events.ClusterType),
HostName=tostring(events.HostName),
LogType=tostring(events.LogType),
OperationName=tostring(events.OperationName),
SourceSystem=tostring(events.SourceSystem),
Type=tostring(events.Type),
_ResourceId=tostring(events._ResourceId),
_TimeReceived=todatetime(now())
}

.alter table HDInsightHiveAndLLAPLogs policy update @'[{"Source": "HDInsightHiveAndLLAPLogsRaw", "Query": "HDInsightHiveAndLLAPLogsExpand()", "IsEnabled": "True", "IsTransactional": true}]'
