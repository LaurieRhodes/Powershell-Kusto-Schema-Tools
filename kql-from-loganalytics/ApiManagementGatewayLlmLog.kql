// ============================================================================
// Azure Data Explorer KQL Script for ApiManagementGatewayLlmLog - UPDATED VERSION
// ============================================================================
// Generated: 2025-09-14 11:22:06
// Table type: Microsoft
// Schema discovered using hybrid approach (Management API + getschema)
// Data type corrections applied: TenantId->guid, Double->real (empirical fixes)
// All columns including underscore columns included
// Original columns: 19, Final columns: 19
// ============================================================================

.create-merge table ApiManagementGatewayLlmLogRaw (records:dynamic)

.alter-merge table ApiManagementGatewayLlmLogRaw policy retention softdelete = 1d

.alter table ApiManagementGatewayLlmLogRaw policy caching hot = 1h

// JSON mapping - choose appropriate option based on data structure
.create-or-alter table ApiManagementGatewayLlmLogRaw ingestion json mapping 'ApiManagementGatewayLlmLogRawMapping' '[{"column":"records","Properties":{"path":"$.records"}}]'
// Alternative for direct events: '[{"column":"records","Properties":{"path":"$"}}]'

.create-merge table ApiManagementGatewayLlmLog(
TimeGenerated:datetime,
TenantId:guid,
OperationName:string,
CorrelationId:string,
Region:string,
SequenceNumber:int,
PromptTokens:int,
CompletionTokens:int,
TotalTokens:int,
ModelName:string,
IsStreamCompletion:bool,
RequestId:string,
DeploymentName:string,
ApiVersion:string,
RequestMessages:dynamic,
ResponseMessages:dynamic,
SourceSystem:string,
Type:string,
_ResourceId:string,
_TimeReceived:datetime)

.alter table ApiManagementGatewayLlmLog policy caching hot = 1d

.create-or-alter function ApiManagementGatewayLlmLogExpand() {
ApiManagementGatewayLlmLogRaw
| mv-expand events = records
// Alternative for non-nested: | extend events = records
| project
TimeGenerated=todatetime(events.TimeGenerated),
TenantId=toguid(events.TenantId),
OperationName=tostring(events.OperationName),
CorrelationId=tostring(events.CorrelationId),
Region=tostring(events.Region),
SequenceNumber=toint(events.SequenceNumber),
PromptTokens=toint(events.PromptTokens),
CompletionTokens=toint(events.CompletionTokens),
TotalTokens=toint(events.TotalTokens),
ModelName=tostring(events.ModelName),
IsStreamCompletion=tobool(events.IsStreamCompletion),
RequestId=tostring(events.RequestId),
DeploymentName=tostring(events.DeploymentName),
ApiVersion=tostring(events.ApiVersion),
RequestMessages=todynamic(events.RequestMessages),
ResponseMessages=todynamic(events.ResponseMessages),
SourceSystem=tostring(events.SourceSystem),
Type=tostring(events.Type),
_ResourceId=tostring(events._ResourceId),
_TimeReceived=todatetime(now())
}

.alter table ApiManagementGatewayLlmLog policy update @'[{"Source": "ApiManagementGatewayLlmLogRaw", "Query": "ApiManagementGatewayLlmLogExpand()", "IsEnabled": "True", "IsTransactional": true}]'
